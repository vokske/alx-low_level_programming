#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main()
{
	char haystack[] = "Get that mullah!";
	char needle[] = "Go get that!";

	_strstr(haystack, needle);
	return (0);
}
